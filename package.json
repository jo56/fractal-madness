{
  "name": "fractal-madness",
  "version": "0.0.4",
  "description": "WebGPU-accelerated fractal visualizer",
  "type": "module",
  "scripts": {
    "wasm:dev": "node scripts/build-wasm.mjs",
    "wasm:release": "node scripts/build-wasm.mjs",
    "dev": "npm run wasm:dev && vite dev",
    "build": "npm run wasm:release && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^7.3.6",
    "vite-plugin-top-level-await": "^1.4.4",
    "vite-plugin-wasm": "^3.6.0"
  },
  "overrides": {
    "uuid": "^11.1.1"
  }
}
